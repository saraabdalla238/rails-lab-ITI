<h1>Articles</h1>
<ul>
  <% @articles.each do |article| %>
    <li> 
    # //instead of ancor ,we can use link to as :- <%link_to 'show',article %>
      <a href="/articles/<%= article.id %>">
        <%= article.title %>
      </a>
    </li>
  <% end %>
</ul>
